# راهنمای گام‌به‌گام ساخت و نصب اپ — بدون نصب هیچ نرم‌افزاری

این راهنما برای کسی نوشته شده که تخصص فنی ندارد و **نمی‌خواهد روی لپ‌تاپش چیزی نصب کند**. تمام کارها در مرورگر انجام می‌شود و ساخت فایل نصبی (APK) روی سرورهای رایگان **GitHub** انجام می‌گیرد.

> اپ فقط برای استفادهٔ شخصی خودتان است. پیشنهاد می‌کنیم مخزن را **Private** بسازید؛ این‌طوری کد، توکن‌ها و فایل نصبی هیچ‌وقت عمومی نمی‌شوند.

## چیزهایی که لازم دارید

| مورد | توضیح |
|---|---|
| یک مرورگر | Chrome / Edge / Firefox روی لپ‌تاپ |
| حساب GitHub | رایگان — [github.com/signup](https://github.com/signup) |
| حساب گوگل | همان که می‌خواهید فایل‌های صوتی روی درایوش برود |
| گوشی اندروید | اندروید ۷ (7.0) یا بالاتر |

هیچ‌کدام از این‌ها لازم نیست: Java، Android Studio، Android SDK، Gradle، برنامهٔ زیپ‌باز کن حرفه‌ای (زیپ‌باز داخلی ویندوز کافی است).

---

## چک‌لیست سریع

- [ ] ۱. ساخت مخزن خصوصی (Private repository)
- [ ] ۲. بارگذاری فایل‌های پروژه
- [ ] ۳. اجرای بیلد و دانلود `app-debug-apk`
- [ ] ۴. برداشتن مقدار SHA-1 از لاگ بیلد
- [ ] ۵. فعال کردن Google Drive API و ساخت OAuth client
- [ ] ۶. نصب APK روی گوشی
- [ ] ۷. وارد کردن Client ID در اپ و اتصال به درایو
- [ ] ۸. استفاده

---

## ۱. ساخت مخزن خصوصی

1. وارد `github.com` شوید و روی دکمهٔ **+** (بالا سمت راست) → **New repository** بزنید.
2. **Repository name**: مثلاً `audio-to-drive`
3. **Private** را انتخاب کنید (مهم است).
4. گزینه‌های **Add a README file** و **Add .gitignore** و **Choose a license** را **دست نزنید** (خالی بمانند). اگر README ساخته شود، بارگذاری فایل‌های شما گیر می‌کند.
5. **Create repository** بزنید.

## ۲. بارگذاری فایل‌های پروژه

1. در صفحهٔ مخزن، روی **Add file → Upload files** بزنید.
2. از پوشهٔ پروژه روی لپ‌تاپ، این‌ها را با موس بکشید و در همان کادر رها کنید (کشیدن پوشه، ساختار داخلش را حفظ می‌کند):
   - پوشهٔ `app`
   - فایل‌های `build.gradle.kts`، `settings.gradle.kts`، `gradle.properties`، `.gitignore`، `README.md`، `ARCHITECTURE.md`، `RAHNAMA.md`
   - اگر می‌توانید پوشهٔ `docs` را هم اضافه کنید (اختیاری، همان صفحهٔ نمودارها است)
3. پایین صفحه، دکمهٔ **Commit changes** را بزنید و صبر کنید تا بارگذاری تمام شود.
4. **مهم:** پوشهٔ `.github` (که با یک نقطه شروع می‌شود) ممکن است در پنجرهٔ انتخاب فایل دیده نشود. پس فایل workflow را دستی بسازید:
   - در مخزن: **Add file → Create new file**
   - در کادر نام فایل، دقیقاً این را تایپ کنید (تایپ اسلش‌ها پوشه می‌سازد):
     ```
     .github/workflows/android-build.yml
     ```
   - محتوای فایل `.github/workflows/android-build.yml` را از پروژهٔ لپ‌تاپ کپی و در کادر بزرگ پیست کنید.
   - **Commit changes** بزنید.
5. بررسی نهایی: در صفحهٔ اصلی مخزن باید پوشه‌های `app`، `.github`، `docs` و فایل‌های ریشه را ببینید.

> همین لحظه که فایل workflow اضافه شود، GitHub به‌طور خودکار یک بیلد شروع می‌کند. اگر این اتفاق افتاد، برو به مرحلهٔ ۳.

## ۳. اجرای بیلد و دانلود فایل نصبی

1. در مخزن، تب **Actions** را باز کنید.
2. اگر بیلدی در حال اجراست، روی نام آن (**Android Build**) کلیک کنید؛ اگر نه:
   - از ستون چپ **Android Build** را انتخاب کنید → دکمهٔ **Run workflow** → **Run workflow**.
3. اجرا بین ۴ تا ۱۰ دقیقه طول می‌کشد (بار اول طولانی‌تر است، چون همهٔ کتابخانه‌ها دانلود می‌شوند). علامت ✅ سبز یعنی موفق.
4. در پایین همان صفحه، بخش **Artifacts** دو فایل می‌بینید:
   - **app-debug-apk** → فایل نصبی اپ (بعد از دانلود، زیپ را Extract کنید تا `app-debug.apk` در بیاید)
   - **keystore** → کلید امضای اپ (آن را در جایی امن نگه دارید؛ برای بیلدهای بعدی لازمتان می‌شود)
5. `app-debug.apk` را به گوشی برسانید: مثلاً آن را در تلگرام «Saved Messages» بفرستید یا در Google Drive بگذارید و از گوشی بازش کنید.

## ۴. برداشتن SHA-1 از لاگ بیلد

1. در صفحهٔ بیلد، روی مرحلهٔ **Print signing report (SHA-1)** کلیک کنید تا باز شود.
2. در متن آبی‌رنگ دنبال خطی بگردید که با **SHA1:** شروع می‌شود؛ چیزی شبیه:
   ```
   SHA1: 3A:1B:9C:...
   ```
3. این مقدار را کپی کنید؛ در مرحلهٔ بعد لازم است.

## ۵. فعال کردن Google Drive API و ساخت OAuth client

1. به [console.cloud.google.com](https://console.cloud.google.com) بروید (با همان حساب گوگل).
2. **بالا، انتخاب پروژه → New Project** → یک نام مثل `Audio to Drive` → **Create**.
3. از منوی سمت چپ: **APIs & Services → Library** → در جست‌وجو بنویسید **Google Drive API** → بازش کنید → **Enable**.
4. از منوی سمت چپ: **APIs & Services → OAuth consent screen**:
   - **External** را انتخاب و **Create** بزنید.
   - **App name**: هرچه دوست دارید (مثلاً `Audio to Drive`).
   - **User support email** و **Developer contact information**: ایمیل خودتان.
   - مراحل بعدی را با **Save and continue** رد کنید (افزودن Scope لازم نیست، اپ خودش هنگام ورود درخواست می‌کند).
   - در بخش **Test users**، حتماً **ایمیل خودتان** را اضافه کنید.
5. از منوی سمت چپ: **APIs & Services → Credentials → Create credentials → OAuth client ID**:
   - **Application type**: **Android**
   - **Package name**: `com.personal.audioapp` (این را دقیقاً همین‌طور بنویسید)
   - **SHA-1**: همان مقداری که در مرحلهٔ ۴ کپی کردید
   - **Create** بزنید.
6. پنجره‌ای باز می‌شود که **Client ID** را نشان می‌دهد (چیزی شبیه `1234567890-abcd.apps.googleusercontent.com`). آن را کپی کنید. (نیازی به دانلود فایل JSON نیست.)

> **اگر بعداً موقع ورود در اپ خطای `redirect_uri_mismatch` یا `invalid_request` دیدید**، به من بگویید (یا از AI دیگری همین را بپرسید): راه‌حل، یک‌بار تغییر یک خط در `gradle.properties` است تا اپ با یک آدرس بازگشت دیگر ساخته شود. مقدار Client ID عوض نمی‌شود.

> **نکتهٔ ۷ روزه:** وقتی وضعیت OAuth consent screen روی **Testing** است، گوگل هر ۷ روز می‌خواهد دوباره وارد شوید. اگر این آزاردهنده بود، در همان صفحهٔ OAuth consent screen دکمهٔ **Publish app** را بزنید؛ پس از آن ورود ماندگار می‌شود (گوگل ممکن است یک هشدار «اپ تأییدنشده» نشان دهد؛ با **Advanced → Go to … (unsafe)** ادامه دهید. برای اپ شخصی مشکلی ندارد).

## ۶. نصب APK روی گوشی

1. فایل `app-debug.apk` را روی گوشی باز کنید.
2. اگر گوشی اجازه نداد: پیام **Settings** را بزنید و برای همان برنامه (فایل‌منیجر یا تلگرام/درایو) گزینهٔ **Allow from this source / Install unknown apps** را روشن کنید، بعد دوباره نصب کنید.
3. ممکن است Play Protect هشدار بدهد؛ **Install anyway** را بزنید (اپ را خودتان ساخته‌اید و امضای شخصی دارد).
4. اگر نسخهٔ قدیمی‌تری نصب است و خطای امضا گرفتید، نسخهٔ قبلی را اول پاک کنید.

## ۷. تنظیم اپ و اتصال به درایو

1. اپ **صدا به درایو** را باز کنید. بار اول اجازهٔ **میکروفون** (و در اندروید ۱۳+ اجازهٔ **نوتیفیکیشن**) را بدهید.
2. آیکن **تنظیمات** (بالا، سمت چپ در چیدمان راست‌چین) را بزنید.
3. در کادر **Client ID** مقدار مرحلهٔ ۵ را پیست کنید.
4. اختیاری: **شناسهٔ پوشهٔ درایو** را خالی بگذارید تا اپ خودش پوشهٔ `AudioApp` را بسازد.
5. **ذخیرهٔ تنظیمات** → **اتصال** → مرورگر باز می‌شود → حساب گوگل را انتخاب کنید.
   - اگر صفحهٔ «این اپ تأییدنشده است» آمد: **Advanced → Go to … (unsafe)**.
   - اگر صفحهٔ دسترسی‌ها آمد: **Continue / Allow**.
6. برگردید به اپ؛ روی کارت بالای صفحه باید «Google Drive متصل است» را ببینید.

## ۸. استفادهٔ روزمره

| کار | چطور |
|---|---|
| ضبط صدا | دکمهٔ گرد پایین صفحه (**ضبط جدید**). برای پایان، همان دکمه را بزنید یا از نوتیفیکیشن دکمهٔ **توقف** را بزنید |
| انتخاب فایل صوتی موجود | دکمهٔ **انتخاب فایل صوتی** |
| پیگیری وضعیت | هر ردیف در صف: **در انتظار → در حال فشرده‌سازی → در حال آپلود → تمام شد** (یا **خطا**) |
| تلاش دوباره پس از خطا | آیکن ⟳ روی همان ردیف |
| پاک کردن یک آیتم | آیکن سبد روی ردیف |
| پاک کردن همهٔ آپلودهای تمام‌شده | دکمهٔ **پاک کردن آپلودهای تمام‌شده** پایین فهرست |

خروجی‌ها: اپ صدا را با **MP3 ۶۴ kbps** فشرده و در پوشهٔ **AudioApp** درایو شما آپلود می‌کند. فایل‌های خام روی گوشی در حافظهٔ خصوصی اپ (`Android/data/com.personal.audioapp/files/`) می‌مانند تا خودتان (یا توگل‌ها) پاکشان کنید.

---

## اگر خطا دیدید

| نشانه | معنی و راه‌حل |
|---|---|
| بیلد در Actions قرمز شد | روی مرحلهٔ قرمز کلیک کنید، متن خطا را برایم بفرستید (یا به AI بدهید). معمولاً یک اصلاح کوچک است |
| در تب Actions نوشته «Workflows aren't being run» | از **Actions → Android Build → Run workflow** دستی اجرا کنید |
| خطای `redirect_uri_mismatch` هنگام اتصال | راه‌حل یک‌خطی است؛ به من بگویید |
| خطای `invalid_client` | Client ID اشتباه پیست شده، یا نوع OAuth client با SHA-1 جور نیست |
| «حساب Google Drive متصل نیست» | در تنظیمات یک‌بار **خروج از حساب** و بعد دوباره **اتصال** را بزنید |
| «آپلود ناموفق بود؛ کد خطای درایو: ۴۰۳/۴۰۴» | اینترنت را چک کنید؛ برای پوشهٔ مقصد، فیلد را خالی بگذارید تا خودش پوشه بسازد |
| «فایل مبدأ وجود ندارد» | فایل خام بعد از فشرده‌سازی پاک شده؛ همان ردیف را Retry بزنید |
| اپ نصب نمی‌شود | نسخهٔ قبلی را پاک کنید و دوباره نصب کنید (امضای APK عوض شده است) |
| هر ۷ روز باید دوباره وارد شوم | وضعیت Testing گوگل است؛ در مرحلهٔ ۵ بخش «Publish app» را ببینید |

**چه چیزی برای من بفرستید:** فقط متن خطا (کپی از لاگ بیلد یا متن پیام داخل اپ). نیازی به توضیح فنی نیست.

---

## برای تغییر دادن چیزها در آینده

هر فایلی را می‌توانید مستقیم در سایت GitHub ویرایش کنید (روی فایل → آیکن مداد → تغییر → **Commit changes**). با هر تغییر، بیلد خودکار اجرا می‌شود و APK تازه در تب Actions آماده می‌شود.

| می‌خواهید… | فایل |
|---|---|
| متن‌های داخل اپ را عوض کنید | `app/src/main/res/values/strings.xml` |
| قالب نام فایل‌های ضبط‌شده | `app/src/main/java/com/personal/audioapp/util/FileUtils.kt` |
| کیفیت/دستور فشرده‌سازی MP3 | `app/src/main/java/com/personal/audioapp/work/CompressionWorker.kt` |
| مسیر/آدرس بازگشت ورود گوگل | `app/src/main/AndroidManifest.xml` و `auth/AuthRepository.kt` |
| فاصلهٔ زمانی و آپلود پس‌زمینه | `app/src/main/java/com/personal/audioapp/work/UploadWorker.kt` |
| جزئیات بیلد (نسخه، امضا، کتابخانه‌ها) | `app/build.gradle.kts` |

**قبل از هر تغییری، این‌ها را عوض نکنید:** نسخهٔ کتابخانه‌ها در `app/build.gradle.kts`، نام بسته `com.personal.audioapp`، و مقادیر `alias`/گذرواژهٔ keystore. توضیح کامل در `ARCHITECTURE.md` بخش ۹ آمده است.
